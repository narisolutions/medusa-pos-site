---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import '../../styles/global.css';

const base = import.meta.env.BASE_URL;
const siteUrl = 'https://narisolutions.github.io/medusa-pos-site';
const publishedDateISO = '2026-03-24';
const modifiedDateISO = '2026-03-24';
const publishedLabel = 'Mar 24, 2026';
const articleUrl = `${siteUrl}${base}blog/getting-started-with-medusa-pos`;
---

<Layout
  title="Getting Started with Medusa POS"
  description="A complete getting started guide for Medusa POS: setup requirements, installation, checkout flow, orders, settings, and FAQ."
>
  <Header variant="blog" />

  <main>
    <article class="editorial-wrapper mx-auto max-w-3xl px-6 py-11 text-left">
      <header class="mb-12 flex flex-col gap-4">
        <a
          href={`${base}blog/overview`}
          class="inline-flex items-center gap-2 rounded-lg border border-[var(--color-border)] bg-[var(--color-surface)] max-w-40 px-4 py-2 text-sm text-[var(--color-text-primary)] transition-colors hover:border-[var(--color-text-secondary)]"
        >
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
          </svg>
          Back to blog
        </a>
      </header>

      <div class="editorial-content">
        <h1>Getting Started with Medusa POS</h1>
        <p class="mb-5 text-sm text-[var(--color-text-secondary)]">
          <time datetime={publishedDateISO}>Published {publishedLabel}</time>
        </p>

        <h2>What you need</h2>
        <p><strong>To get started, you will need:</strong></p>
        <ul>
          <li>A Windows, macOS, or Linux computer at the checkout counter</li>
          <li>
            A running <a href="https://medusajs.com/">Medusa</a> store (v2.13.x) with products and a sales channel set up
          </li>
          <li>Your Medusa Admin login (email and password)</li>
        </ul>
        <p><strong>Optional hardware (recommended for a full retail setup):</strong></p>
        <ul>
          <li>
            A <strong>network thermal printer</strong> - an 80 mm receipt printer connected to your Wi-Fi or Ethernet
            network (for example, Epson TM-series or BIXOLON models)
          </li>
          <li>
            A <strong>USB barcode scanner</strong> - any standard scanner that plugs into a USB port and acts like a
            keyboard
          </li>
          <li>
            A <strong>cash drawer</strong> - connected to your thermal printer via the standard RJ-11 cable
          </li>
        </ul>

        <hr />

        <h2>Get started - setup in minutes</h2>
        <ol>
          <li>
            <strong>Download and install</strong> the app from <a href="https://pos.nari.ge/">pos.nari.ge</a> - choose
            the version for your operating system.
          </li>
          <li>
            <strong>Enter your store address</strong> - on first launch, the app asks for your Medusa backend URL
            (for example, <code class="rounded bg-[var(--color-bg)] px-1.5 py-0.5 text-xs">https://admin.mystore.com</code>). Type it
            in and save. You can add more stores later under Settings.
          </li>
          <li>
            <strong>Sign in</strong> with your Medusa Admin email and password.
          </li>
          <li>
            <strong>Add your printer</strong> - open <strong>Settings -> Printers</strong>, tap <strong>Add printer</strong>,
            enter the printer's IP address, and run a test print to confirm it is working.
          </li>
          <li>
            <strong>Test the cash drawer</strong> - from the same printer settings screen, tap <strong>Test drawer</strong>.
            If a drawer is connected to the printer, it should pop open.
          </li>
          <li>
            <strong>Start selling</strong> - go to the Checkout screen, search or scan your first product, and you are ready.
          </li>
        </ol>

        <div data-theme-view="light">
          <figure class="editorial-figure">
            <img src={`${base}article/lightStoreSelect.png`} alt="Choose your store at login" draggable="false" />
            <figcaption>Choose your store at login</figcaption>
          </figure>
        </div>
        <div data-theme-view="dark">
          <figure class="editorial-figure">
            <img src={`${base}article/darkStoreSelect.png`} alt="Choose your store at login" draggable="false" />
            <figcaption>Choose your store at login</figcaption>
          </figure>
        </div>

        <div data-theme-view="light">
          <figure class="editorial-figure">
            <img src={`${base}article/lightLogin.png`} alt="Sign in with your Medusa Admin credentials" draggable="false" />
            <figcaption>Sign in with your Medusa Admin credentials</figcaption>
          </figure>
        </div>
        <div data-theme-view="dark">
          <figure class="editorial-figure">
            <img src={`${base}article/darkLogin.png`} alt="Sign in with your Medusa Admin credentials" draggable="false" />
            <figcaption>Sign in with your Medusa Admin credentials</figcaption>
          </figure>
        </div>

        <hr />

        <h2>Daily checkout flow</h2>
        <p>Once you are set up, a typical sale takes about 30 seconds:</p>
        <ol>
          <li>
            <strong>Pick your store and sign in.</strong> If you have more than one location, choose the right one
            from the dropdown, then enter your credentials.
          </li>
          <li>
            <strong>Find and add products.</strong> On the Checkout screen, scan the barcode on the product packaging
            or type the name in the search bar. Tap to add each item to the cart.
          </li>
        </ol>

        <div data-theme-view="light">
          <figure class="editorial-figure">
            <img src={`${base}article/lightCheckout.png`} alt="The checkout screen - search, cart, and total in one view" draggable="false" />
            <figcaption>The checkout screen - search, cart, and total in one view</figcaption>
          </figure>
        </div>
        <div data-theme-view="dark">
          <figure class="editorial-figure">
            <img src={`${base}article/darkCheckout.png`} alt="The checkout screen - search, cart, and total in one view" draggable="false" />
            <figcaption>The checkout screen - search, cart, and total in one view</figcaption>
          </figure>
        </div>

        <ol start="3">
          <li>
            <strong>Review the cart.</strong> Adjust quantities or apply a discount if needed. The order total updates immediately.
          </li>
          <li>
            <strong>Tap Charge.</strong> A payment window opens. Enter the cash received - the change is calculated for you - then confirm.
          </li>
        </ol>

        <div data-theme-view="light">
          <figure class="editorial-figure">
            <img src={`${base}article/lightPaymentModal.png`} alt="Enter cash received; change is shown automatically" draggable="false" />
            <figcaption>Enter cash received; change is shown automatically</figcaption>
          </figure>
        </div>
        <div data-theme-view="dark">
          <figure class="editorial-figure">
            <img src={`${base}article/darkPaymentModal.png`} alt="Enter cash received; change is shown automatically" draggable="false" />
            <figcaption>Enter cash received; change is shown automatically</figcaption>
          </figure>
        </div>

        <ol start="5">
          <li>
            <strong>Receipt prints; drawer opens if configured.</strong> The receipt prints right after payment. If a cash drawer is connected and enabled in your printer settings, it opens at the same time.
          </li>
          <li>
            <strong>Order saved.</strong> The completed sale appears in your Medusa order history immediately.
          </li>
        </ol>

        <hr />

        <h2>Checking past orders</h2>
        <p>
          Every completed transaction is stored and searchable. Tap <strong>Orders</strong> in the sidebar to browse your
          sales history, use the search bar or filter chips to find a specific order, and tap any row to see the
          full detail - items, totals, and payment method.
        </p>
        <p>
          You can reprint a receipt from the order detail screen at any time.
        </p>

        <div data-theme-view="light">
          <figure class="editorial-figure">
            <img src={`${base}article/lightOrdersList.png`} alt="Browse and search every past order" draggable="false" />
            <figcaption>Browse and search every past order</figcaption>
          </figure>
        </div>
        <div data-theme-view="dark">
          <figure class="editorial-figure">
            <img src={`${base}article/darkOrdersList.png`} alt="Browse and search every past order" draggable="false" />
            <figcaption>Browse and search every past order</figcaption>
          </figure>
        </div>

        <div data-theme-view="light">
          <figure class="editorial-figure">
            <img src={`${base}article/lightOrder.png`} alt="Full order detail with option to reprint" draggable="false" />
            <figcaption>Full order detail with option to reprint</figcaption>
          </figure>
        </div>
        <div data-theme-view="dark">
          <figure class="editorial-figure">
            <img src={`${base}article/darkOrder.png`} alt="Full order detail with option to reprint" draggable="false" />
            <figcaption>Full order detail with option to reprint</figcaption>
          </figure>
        </div>

        <hr />

        <h2>Settings and printers</h2>
        <p>Everything you need to configure lives under <strong>Settings</strong>:</p>
        <ul>
          <li>
            <strong>Store</strong> - add, rename, or remove Medusa backend addresses. Multi-location shops can add each store here and switch between them at login.
          </li>
          <li>
            <strong>Printers</strong> - add a printer by entering its IP address and port. Mark a default, run a test print, and test the cash drawer trigger from the same screen. Network printers (Wi-Fi or Ethernet) are the most reliable option.
          </li>
          <li>
            <strong>Preferences</strong> - choose your display theme (Light, Dark, or System), currency symbol, and date format.
          </li>
          <li>
            <strong>Branding</strong> - set your company name, address, phone number, and upload a logo. These appear on every printed receipt.
          </li>
        </ul>

        <div data-theme-view="light">
          <figure class="editorial-figure">
            <img src={`${base}article/lightPrinterSettings.png`} alt="Configure and test your printer in Settings" draggable="false" />
            <figcaption>Configure and test your printer in Settings</figcaption>
          </figure>
        </div>
        <div data-theme-view="dark">
          <figure class="editorial-figure">
            <img src={`${base}article/darkPrinterSettings.png`} alt="Configure and test your printer in Settings" draggable="false" />
            <figcaption>Configure and test your printer in Settings</figcaption>
          </figure>
        </div>

        <div data-theme-view="light">
          <figure class="editorial-figure">
            <img src={`${base}article/lightPreferencesSettings.png`} alt="Set your theme, currency, and date format" draggable="false" />
            <figcaption>Set your theme, currency, and date format</figcaption>
          </figure>
        </div>
        <div data-theme-view="dark">
          <figure class="editorial-figure">
            <img src={`${base}article/darkPreferencesSettings.png`} alt="Set your theme, currency, and date format" draggable="false" />
            <figcaption>Set your theme, currency, and date format</figcaption>
          </figure>
        </div>

        <hr />

        <h2>FAQ</h2>
        <p><strong>Why does Windows or macOS show a warning when I install the app?</strong></p>
        <p>
          The open-source release is not signed with a paid commercial certificate, so your operating system shows
          a security prompt the first time you run it. If you downloaded the file from <a href="https://pos.nari.ge/">pos.nari.ge</a>, you can safely proceed. Businesses deploying to many machines can apply their own code-signing certificate to the build.
        </p>
        <p><strong>What printers are compatible?</strong></p>
        <p>
          Any 80 mm thermal receipt printer that accepts connections over a network (Wi-Fi or Ethernet) on port 9100 - for example, Epson TM-series or BIXOLON printers. <em>Thermal printer = the till-roll type that prints receipts without ink cartridges.</em> USB and Bluetooth connections are experimental and may not work on all systems yet.
        </p>
        <p><strong>Is barcode scanning required?</strong></p>
        <p>
          No - it is completely optional. Any product can be found and added using the search bar. If you plug in a USB barcode scanner, it is detected automatically. Full barcode-to-product lookup additionally requires an optional companion plugin installed on your Medusa server.
        </p>
        <p><strong>Does the cash drawer always open after a sale?</strong></p>
        <p>
          It opens when configured correctly: the cash drawer must be connected to your thermal printer via the standard cable, and the printer must be set up in <strong>Settings -> Printers</strong>. When those conditions are met, the app sends the open command to the printer at the end of a completed sale.
        </p>
        <p><strong>Can I use Medusa POS on a tablet or phone?</strong></p>
        <p>
          An experimental Android build is available at <a href="https://pos.nari.ge/">pos.nari.ge</a>. The desktop version (Windows, macOS, Linux) is the most stable and recommended for everyday retail use.
        </p>

        <section class="mt-12">
          <h2>Where to go next</h2>
          <div class="mt-6 flex flex-wrap gap-3">
            <a
              href={`${base}#downloads`}
              class="rounded-lg bg-[var(--color-accent)] px-5 py-2.5 font-medium no-underline transition-colors hover:bg-[var(--color-accent-hover)]"
              style="color: #fff;"
            >
              Download Medusa POS
            </a>
            <a
              href="https://github.com/narisolutions/medusa-pos"
              target="_blank"
              rel="noopener noreferrer"
              class="rounded-lg border border-[var(--color-border)] px-5 py-2.5 font-medium text-[var(--color-text-primary)] transition-colors hover:border-[var(--color-text-secondary)]"
            >
              View on GitHub
            </a>
          </div>
        </section>
      </div>

      <!-- Lightbox (zoom on click for article screenshots) -->
      <div id="blog-lightbox" class="lightbox is-hidden" role="dialog" aria-modal="true" aria-label="Screenshot viewer">
        <div id="blog-lightbox-backdrop" class="lightbox-backdrop"></div>
        <button id="blog-lightbox-close" class="lightbox-close" aria-label="Close">
          <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M18 6L6 18M6 6l12 12" />
          </svg>
        </button>

        <div class="lightbox-inner">
          <button id="blog-lightbox-prev" class="lightbox-nav" aria-label="Previous screenshot">
            <svg width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
            </svg>
          </button>
          <img id="blog-lightbox-img" src="" alt="" class="lightbox-img" draggable="false" />
          <button id="blog-lightbox-next" class="lightbox-nav" aria-label="Next screenshot">
            <svg width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
            </svg>
          </button>
        </div>
        <p id="blog-lightbox-counter" class="lightbox-counter"></p>
      </div>

      <style is:global>
        .lightbox {
          position: fixed;
          inset: 0;
          z-index: 9999;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
        }
        .lightbox.is-hidden { display: none; }
        .lightbox-backdrop {
          position: absolute;
          inset: 0;
          background: rgba(0, 0, 0, 0.88);
          cursor: pointer;
        }
        .lightbox-close {
          position: absolute;
          top: 16px;
          right: 16px;
          z-index: 1;
          width: 40px;
          height: 40px;
          border-radius: 50%;
          background: rgba(255, 255, 255, 0.1);
          border: 1px solid rgba(255, 255, 255, 0.2);
          color: #fff;
          cursor: pointer;
          display: flex;
          align-items: center;
          justify-content: center;
          transition: background 150ms;
        }
        .lightbox-close:hover { background: rgba(255, 255, 255, 0.22); }
        .lightbox-inner {
          position: relative;
          z-index: 1;
          display: flex;
          align-items: center;
          gap: 16px;
        }
        .lightbox-img {
          max-width: calc(100vw - 160px);
          max-height: calc(100vh - 96px);
          width: auto;
          height: auto;
          object-fit: contain;
          border-radius: 8px;
          box-shadow: 0 24px 64px rgba(0, 0, 0, 0.6);
          display: block;
        }
        .lightbox-nav {
          flex-shrink: 0;
          width: 48px;
          height: 48px;
          border-radius: 50%;
          background: rgba(255, 255, 255, 0.1);
          border: 1px solid rgba(255, 255, 255, 0.2);
          color: #fff;
          cursor: pointer;
          display: flex;
          align-items: center;
          justify-content: center;
          transition: background 150ms;
        }
        .lightbox-nav:hover { background: rgba(255, 255, 255, 0.22); }
        .lightbox-counter {
          position: absolute;
          bottom: 20px;
          left: 50%;
          transform: translateX(-50%);
          z-index: 1;
          color: rgba(255, 255, 255, 0.6);
          font-size: 13px;
          letter-spacing: 0.05em;
          pointer-events: none;
        }
      </style>

      <script is:inline>
        (function () {
          var lightbox = document.getElementById('blog-lightbox');
          if (!lightbox) return;

          var lightboxImg = document.getElementById('blog-lightbox-img');
          var lightboxCounter = document.getElementById('blog-lightbox-counter');
          var lightboxClose = document.getElementById('blog-lightbox-close');
          var lightboxPrev = document.getElementById('blog-lightbox-prev');
          var lightboxNext = document.getElementById('blog-lightbox-next');
          var lightboxBackdrop = document.getElementById('blog-lightbox-backdrop');

          var lbImages = [];
          var lbIndex = 0;

          function activeTheme() {
            return document.documentElement.dataset.theme || 'dark';
          }

          function getImagesForActiveTheme() {
            var theme = activeTheme();
            return Array.from(
              document.querySelectorAll(
                '.editorial-content [data-theme-view="' +
                  theme +
                  '"] figure.editorial-figure img'
              )
            );
          }

          function updateLightbox() {
            if (!lbImages.length) return;
            var img = lbImages[lbIndex];
            lightboxImg.src = img.src;
            lightboxImg.alt = img.alt || '';
            lightboxCounter.textContent = lbIndex + 1 + ' / ' + lbImages.length;
          }

          function openLightbox(images, index) {
            lbImages = images;
            lbIndex = index;
            updateLightbox();
            lightbox.classList.remove('is-hidden');
            document.body.style.overflow = 'hidden';
          }

          function closeLightbox() {
            lightbox.classList.add('is-hidden');
            document.body.style.overflow = '';
          }

          function lbNav(dir) {
            if (!lbImages.length) return;
            lbIndex = (lbIndex + dir + lbImages.length) % lbImages.length;
            updateLightbox();
          }

          var allScreenshotImgs = Array.from(
            document.querySelectorAll('.editorial-content [data-theme-view] figure.editorial-figure img')
          );

          allScreenshotImgs.forEach(function (img) {
            img.addEventListener('click', function () {
              var images = getImagesForActiveTheme();
              if (!images.length) return;
              var index = images.findIndex(function (x) {
                return x.src === img.src;
              });
              openLightbox(images, index >= 0 ? index : 0);
            });
          });

          if (lightboxClose) lightboxClose.addEventListener('click', closeLightbox);
          if (lightboxBackdrop) lightboxBackdrop.addEventListener('click', closeLightbox);
          if (lightboxPrev) lightboxPrev.addEventListener('click', function () { lbNav(-1); });
          if (lightboxNext) lightboxNext.addEventListener('click', function () { lbNav(1); });

          document.addEventListener('keydown', function (e) {
            if (!lightbox || lightbox.classList.contains('is-hidden')) return;
            if (e.key === 'Escape') closeLightbox();
            else if (e.key === 'ArrowLeft') lbNav(-1);
            else if (e.key === 'ArrowRight') lbNav(1);
          });
        })();
      </script>

      <script
        type="application/ld+json"
        set:html={JSON.stringify({
          '@context': 'https://schema.org',
          '@type': 'BlogPosting',
          headline: 'Getting Started with Medusa POS',
          datePublished: publishedDateISO,
          dateModified: modifiedDateISO,
          description:
            'A complete getting started guide for Medusa POS: setup requirements, installation, checkout flow, orders, settings, and FAQ.',
          url: articleUrl,
          mainEntityOfPage: articleUrl,
          author: {
            '@type': 'Organization',
            name: 'Nari Solutions',
            url: 'https://nari.ge',
          },
          publisher: {
            '@type': 'Organization',
            name: 'Nari Solutions',
          },
        })}
      />
    </article>
  </main>

  <Footer compact />
</Layout>
